# pytest.ini
[pytest]
addopts = -ra -q --strict-markers --disable-warnings
testpaths = tests
markers =
    xfail_bug: expected to fail due to known intentional bug
    integration: integration tests that use the Flask test client
